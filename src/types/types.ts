type UserInfo = {
    name: string;
    email: string;
};

type DogType = {
  id: string
  img: string
  name: string
  age: number
  zip_code: string
  breed: string
};

export { DogType, UserInfo };